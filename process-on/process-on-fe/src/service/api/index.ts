// eslint-disable-next-line object-curly-newline
import { uPost, uFetchAll, uGet, uPut, uDelete } from './utils';

const Api = {
  uPost,
  uFetchAll,
  uGet,
  uPut,
  uDelete,
};

export default Api;
